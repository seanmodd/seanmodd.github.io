window.PCKUP_GUIDE_PHASES = (window.PCKUP_GUIDE_PHASES || []).concat([
  {
    "id": "phase-0",
    "number": "00",
    "stage": "DISCOVER",
    "title": "Program bootstrap and design-debt freeze",
    "summary": "Create the migration control plane, establish immutable guardrails, and prevent the codebase from accumulating new rogue UI while the migration is underway.",
    "dependsOn": "None",
    "duration": "1 focused PR",
    "outputs": [
      "Program charter and authority model",
      "Persistent migration state and ledger",
      "Baseline design-debt metrics",
      "Warning-only new-debt checks",
      "Approved phase sequence and risk model"
    ],
    "gate": "The program has a reviewed charter, current-state baseline, warning-only debt detectors, and a resumable state ledger. No production visual or behavioral change occurred.",
    "prompt": "Execute Phase 00: Program bootstrap and design-debt freeze.\n\nObjective:\nCreate the durable control plane for a multi-month, incremental migration of the existing Pckup frontend into a scalable, canonical design-system architecture. This phase must establish governance and observability before any component or CSS refactor begins.\n\nRequired work:\n1. Inspect the current repository and all prior design-system work, including at minimum:\n   - docs/design-system-reconstruction/**\n   - docs/DESIGN_SYSTEM_SYNC.md\n   - docs/DESIGN_TOKEN_SYNC_CONTRACT.md\n   - docs/brand-assets/tokens/**\n   - figma.config.json and figma/**\n   - scripts/design-tokens/**\n   - src/app/styles/**\n   - src/app/components/**\n   - route-level component directories under Marketing, Auth, Estimate, Dashboard, Courier, and Utility.\n2. Reconcile prior conclusions with current main. Treat old reports as evidence, not current truth.\n3. Create docs/design-system-migration/program-charter.md defining:\n   - problem statement;\n   - in-scope and out-of-scope work;\n   - authority model for Git, Figma, tokens, and Code Connect;\n   - no-big-bang migration principle;\n   - migration-unit definition;\n   - visual-change approval rule;\n   - deletion rule;\n   - security and accessibility invariants;\n   - completion metrics.\n4. Create a machine-readable state.json and migration-ledger.json schema. Include phase status, current baseline commit, completed units, active unit, blocked decisions, artifacts, metrics, Figma mappings, and next action.\n5. Create handoff.md as the one-page resume entrypoint for future agents.\n6. Add warning-only scanners or reports for new design debt. They must not fail CI yet. At minimum measure:\n   - raw button/input/select/textarea usage outside approved primitive locations;\n   - inline visual style usage;\n   - new global component selectors;\n   - CSS file size and selector counts;\n   - oversized UI component files;\n   - imports from canonical UI components;\n   - Code Connect coverage for canonical components.\n7. Record the current baseline rather than pretending it is already clean. A warning must distinguish inherited debt from newly introduced debt.\n8. Define the migration stages and prerequisite graph used by this guide. Each later phase must be blocked until its predecessor's gate passes.\n9. Add no product redesign, no Figma mutation, and no production-facing component refactor in this phase.\n\nValidation:\n- Run the full task-prescribed checks.\n- Prove warning-only scanners are deterministic on two consecutive runs.\n- Prove they detect a temporary planted violation, then remove the fixture.\n- Confirm the application output is unchanged.\n\nExit gate:\nThe program charter, state ledger, handoff, baseline metrics, and warning-only debt detectors exist and are validated. The next agent can resume using only handoff.md and state.json."
  },
  {
    "id": "phase-1",
    "number": "01",
    "stage": "DISCOVER",
    "title": "Repository-wide design architecture census",
    "summary": "Build a machine-readable map of every design-relevant React component, raw control, selector, route surface, state, and existing Figma relationship.",
    "dependsOn": "Phase 00",
    "duration": "1-2 focused PRs",
    "outputs": [
      "Component and route census",
      "CSS selector ownership graph",
      "Raw-control and inline-style inventory",
      "Runtime screenshot matrix",
      "Code-to-Figma coverage map"
    ],
    "gate": "Every user-facing route family and design-relevant code path is represented in a machine-readable census with ownership and confidence. Coverage gaps are explicit.",
    "prompt": "Execute Phase 01: Repository-wide design architecture census.\n\nObjective:\nProduce the complete evidence base for the migration. Do not limit the audit to src/app/components/ui. Inventory the entire design-relevant frontend, including route-local and feature-local components, global CSS conventions, and runtime surfaces.\n\nRequired work:\n1. Build or extend deterministic scanners under scripts/design-system-migration/ that inspect the current repository using AST-aware parsing where practical.\n2. Inventory all React/TSX components and exports under src/app and other frontend source locations. Record:\n   - file path and export name;\n   - client/server boundary;\n   - props and variant-like unions;\n   - native interactive elements rendered;\n   - className literals and composition patterns;\n   - inline style properties;\n   - imported canonical components;\n   - routes and call sites;\n   - approximate reuse count;\n   - file size and line count.\n3. Inventory every CSS file and selector. Record:\n   - stylesheet and selector;\n   - selector family/prefix;\n   - declarations and token usage;\n   - likely owning components and routes;\n   - duplicate or near-duplicate declaration fingerprints;\n   - media-query ownership;\n   - hardcoded visual values already covered by the existing literal baseline;\n   - selectors with no observed code owner and code classes with no selector.\n4. Build a route-surface matrix covering at minimum:\n   - Marketing;\n   - Authentication;\n   - Estimate/order flow;\n   - Customer dashboard;\n   - Courier application;\n   - Courier workspace;\n   - Internal Utility and every major Utility product family;\n   - shared overlays, consent UI, public navigation, and payment-link surfaces.\n5. Capture representative runtime screenshots or existing approved evidence for desktop, mobile, light, dark, loading, empty, error, disabled, open, and busy states where available. Do not invent inaccessible states.\n6. Join the code census to the live Figma library and existing design-system contracts. For each known Figma component, record node ID, published status, current code target, Code Connect state, and whether it is canonical, prototype-only, duplicate, or unverified.\n7. Produce at minimum:\n   - component-census.json;\n   - selector-ownership.json;\n   - route-surface-matrix.json;\n   - interaction-inventory.json;\n   - figma-coverage.json;\n   - a concise human-readable census report.\n8. Assign confidence levels to inferred ownership. Do not present fuzzy matches as facts.\n9. Do not refactor components, split CSS, or mutate Figma in this phase.\n\nValidation:\n- Every top-level frontend route family must be accounted for.\n- Scanner output must be stable across repeated runs.\n- Manually verify a stratified sample from every surface.\n- Reconcile counts against repository tree and existing LOC reports.\n- List all known blind spots explicitly.\n\nExit gate:\nThe migration has a reproducible, current, whole-codebase design census. No later component decision relies on a partial src/app/components/ui-only view."
  },
  {
    "id": "phase-2",
    "number": "02",
    "stage": "DEFINE",
    "title": "Equivalence graph and component taxonomy",
    "summary": "Cluster the current implementation into real component families, distinguish reuse from coincidence, and decide what should be canonical, local, compositional, or retired.",
    "dependsOn": "Phase 01",
    "duration": "1 architecture PR",
    "outputs": [
      "Visual and behavioral equivalence graph",
      "Canonical family taxonomy",
      "Duplicate and retirement decisions",
      "Decision-fork register",
      "Prioritized migration backlog"
    ],
    "gate": "Every reusable pattern has a disposition and target family. Figma scope is locked to canonical families, not a scrape of current rogue implementations.",
    "prompt": "Execute Phase 02: Equivalence graph and component taxonomy.\n\nObjective:\nTurn the census into design-system decisions. Identify which current implementations represent the same concept, which differences are meaningful product requirements, and which patterns should never become shared components.\n\nRequired work:\n1. Build an equivalence graph using code anatomy, props, runtime appearance, CSS fingerprints, states, accessibility behavior, and call-site semantics.\n2. Start with the highest-duplication families:\n   - buttons, icon buttons, links styled as buttons, menu toggles, and pill controls;\n   - text fields, textareas, selects, checkboxes, radios, password, phone, address, upload, and search controls;\n   - dialogs, drawers, popovers, disclosure panels, account menus, and confirmation flows;\n   - badges, statuses, notices, banners, empty/error/loading states;\n   - cards, panels, metric cards, record cards, table rows, and mobile row cards;\n   - page headers, filter bars, pagination, tabs, navigation items, and shells.\n3. Classify every design-relevant pattern as exactly one of:\n   - Core: reusable across multiple product families;\n   - Surface: reusable inside one major surface family such as Product or Operations;\n   - Feature-local: reusable only within one feature;\n   - Page composition: assembly of components, not a library component;\n   - Duplicate/retired: migrate and delete;\n   - Unresolved: genuine decision fork requiring owner input.\n4. For each proposed canonical family, define:\n   - purpose and anti-purpose;\n   - expected consumers;\n   - anatomy;\n   - behavior and accessibility invariants;\n   - proposed props and variants;\n   - existing implementations to absorb;\n   - existing Figma assets to keep, revise, merge, or retire;\n   - migration risk and recommended order.\n5. Explicitly analyze known duplicates, including ConfirmDialog versus UtilityDialog and DashboardShell versus CourierWorkspaceShell. Do not assume identical appearance means identical behavior, and do not preserve duplicate foundations merely because behavior differs.\n6. Avoid premature abstraction. Require a real reuse contract or system-level consistency need.\n7. Create:\n   - equivalence-graph.json;\n   - taxonomy.md;\n   - family-decisions.json;\n   - retirement-register.json;\n   - decision-forks.md;\n   - prioritized-migration-backlog.md.\n8. Resolve objective conflicts in this phase. Surface only genuine product or brand choices to the owner.\n9. Do not implement component refactors or mutate Figma.\n\nValidation:\n- Trace at least three representative call sites into every proposed Core family.\n- Prove every current census item has a disposition.\n- Review for over-abstraction and under-consolidation separately.\n- Have an independent review challenge the family boundaries and retirement decisions.\n\nExit gate:\nThe future library scope is locked. The team knows what will become canonical, what remains local, what is only composition, and what will be deleted."
  },
  {
    "id": "phase-3",
    "number": "03",
    "stage": "DEFINE",
    "title": "Target architecture and source-of-truth contracts",
    "summary": "Define the scalable code, CSS, Figma, ownership, and release architecture before extracting any production component.",
    "dependsOn": "Phase 02",
    "duration": "1 architecture PR",
    "outputs": [
      "Target directory and package architecture",
      "Component API and styling standards",
      "Figma library layering model",
      "Source-of-truth matrix",
      "Migration-unit contract"
    ],
    "gate": "The target architecture is concrete enough that two independent agents would place and build the same component in the same way.",
    "prompt": "Execute Phase 03: Target architecture and source-of-truth contracts.\n\nObjective:\nDefine the destination architecture in enough detail that component extraction becomes mechanical and consistent. This is a migration architecture for the existing codebase, not a generic design-system essay.\n\nRequired work:\n1. Propose and validate a target source layout that separates:\n   - foundations/tokens/icons;\n   - core primitives;\n   - shared components;\n   - product or operations patterns;\n   - shells;\n   - feature-local UI;\n   - route composition;\n   - generated artifacts;\n   - temporary legacy compatibility.\n2. Evaluate moving reusable design-system code out of src/app into a stable source boundary such as src/design-system or an equivalent structure justified by the repo. Account for Next.js App Router, server/client boundaries, path aliases, tree shaking, tests, and existing imports.\n3. Define the standard component folder contract. Include, where appropriate:\n   - Component.tsx;\n   - Component.module.css or the chosen equivalent;\n   - Component.test.tsx;\n   - Component.stories or the chosen reference harness artifact;\n   - Component.figma.ts;\n   - index.ts;\n   - README or metadata manifest only when useful.\n4. Decide the styling ownership model. The default should make selector ownership local and explicit. Explain the limited remaining role of global CSS and legacy import facades.\n5. Define component API standards:\n   - prop naming;\n   - variants versus booleans;\n   - controlled/uncontrolled behavior;\n   - slots and composition;\n   - polymorphic links/buttons;\n   - loading and disabled semantics;\n   - accessibility;\n   - test and visual state requirements.\n6. Define Figma layering:\n   - Foundations;\n   - Core;\n   - Product;\n   - Operations;\n   with dependency direction and publication rules. This can begin as pages/sections in one file if that is operationally safer, but the dependency model must be explicit.\n7. Define the source-of-truth matrix for tokens, component behavior, component anatomy, product composition, Code Connect, and deployed truth.\n8. Define a migration-unit contract. Each unit must include discovery, target API, implementation, call-site migration, Figma disposition, Code Connect disposition, tests, screenshots, metrics, rollback, and legacy deletion criteria.\n9. Evaluate Storybook versus extending the existing /admin/design-system and Utility Lab reference infrastructure. Do not adopt Storybook by default. Choose based on repository fit, public/private boundaries, server component support, visual testing needs, and maintenance cost.\n10. Record accepted decisions as ADRs under docs/design-system-migration/decisions/.\n11. Do not migrate production components or mutate Figma yet.\n\nValidation:\n- Walk Button, Dialog, DataTable, and PortalShell through the proposed architecture as paper exercises.\n- Verify no circular package or library dependency.\n- Verify auth/security boundaries remain enforceable.\n- Obtain independent architecture and security reviews.\n\nExit gate:\nThe destination architecture, ownership rules, component contract, Figma layering, and migration-unit definition are approved and unambiguous."
  },
  {
    "id": "phase-4",
    "number": "04",
    "stage": "ENABLE",
    "title": "Migration harness and canonical reference environment",
    "summary": "Create the tooling that proves migration units are safe: reference rendering, visual baselines, usage metrics, codemod support, and drift reporting.",
    "dependsOn": "Phase 03",
    "duration": "1-2 infrastructure PRs",
    "outputs": [
      "Canonical component reference environment",
      "Visual regression matrix",
      "Usage and adoption metrics",
      "Migration-unit templates",
      "Warning-only CI reports"
    ],
    "gate": "A component can be migrated with automated usage counts, state rendering, visual evidence, and rollback proof before product-wide extraction begins.",
    "prompt": "Execute Phase 04: Migration harness and canonical reference environment.\n\nObjective:\nBuild the safety system required for incremental migration. Do not begin broad component extraction until the repository can render, measure, compare, and review canonical components independently.\n\nRequired work:\n1. Implement the reference environment chosen in Phase 03. Reuse existing /admin/design-system and Utility Lab infrastructure when that is the approved choice; otherwise implement the approved alternative.\n2. Ensure the reference environment can render canonical component states across:\n   - light and dark themes;\n   - desktop and mobile widths;\n   - default, hover, focus, disabled, loading, empty, error, open, busy, and selected states where real;\n   - representative content extremes.\n3. Build deterministic screenshot capture for component states and representative route surfaces. Reuse existing Playwright/e2e infrastructure.\n4. Add a migration manifest and component registry that can report:\n   - canonical component families;\n   - implementation path;\n   - owners/scope;\n   - Figma node and publication status;\n   - Code Connect status;\n   - usage count;\n   - legacy implementations remaining;\n   - test and screenshot coverage.\n5. Implement deterministic usage-count tooling that can distinguish imports, raw native controls, class-pattern usage, and false positives.\n6. Add migration-unit templates for spec, checklist, evidence, review, rollback, and handoff.\n7. Add helper codemods only where transformations are safe and inspectable. Every codemod must support dry run and produce a reviewable file list.\n8. Add warning-only CI summaries for adoption and debt. Do not block inherited debt yet.\n9. Preserve current product output. The new reference surface must be access-controlled consistently with existing internal design-system documentation unless Phase 03 explicitly approved another boundary.\n10. Do not mutate Figma in this phase.\n\nValidation:\n- Render a pilot existing canonical component, such as Button, in every supported state.\n- Capture and compare screenshots deterministically.\n- Prove usage counts against a manual sample.\n- Prove a codemod dry run makes no writes.\n- Prove the harness catches an intentional visual or API mismatch, then remove the fixture.\n\nExit gate:\nThe migration harness can safely support one component-family extraction from baseline through review, adoption measurement, and rollback."
  }
]);
